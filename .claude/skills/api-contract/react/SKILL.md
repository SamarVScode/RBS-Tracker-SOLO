# API Contract — React / Vite

All types in `src/types/`. Service layer interfaces define what the frontend calls.
Firebase callable function shapes define what Functions return.
No server-side types needed — client SDK only.
