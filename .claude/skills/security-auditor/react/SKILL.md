# Security — React

- CORS configuration for Firebase Functions calls
- Firebase API key restricted in Firebase Console
- No sensitive data in Zustand stores that persist to localStorage
- Service worker not caching auth tokens
